sync:
  vagrant-mutagen-node1:
    alpha: '.'
    beta: 'vagrant-mutagen-node1:/home/vagrant/vagrant-mutagen'
    mode: 'two-way-resolved'
    flushOnCreate: true
    ignore:
      vcs: true
    configurationBeta:
      permissions:
        defaultOwner: vagrant
        defaultGroup: vagrant
        defaultFileMode: 644
        defaultDirectoryMode: 755
